---
name: local-advisor-init
description: Create the native Cursor local-advisor JSONC config for this project.
disable-model-invocation: true
---
# Local advisor init
Run `python ./cli/advisor_init.py --workspace <PROJECT_ROOT>` from the installed plugin root. Use `--force` only if the user explicitly wants to replace an existing config. Copy stdout verbatim.
