---
name: local-advisor-model
description: View or persist the native Cursor local advisor model for this project.
argument-hint: "[model]"
disable-model-invocation: true
---
# Local advisor model
Run `python ./cli/advisor_model.py --model <USER_VALUE> --workspace <PROJECT_ROOT>`. With no value, use `--model` only and present the menu. Cursor supplies the model-specific effort defaults; there is no effort setting.
