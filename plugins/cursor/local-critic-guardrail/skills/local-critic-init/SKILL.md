---
name: local-critic-init
description: Create the commented local critic project configuration.
---
# Local critic init

Run `uv run --no-project python ./cli/critic_init.py --workspace <PROJECT_ROOT>`. Use `--force` only when the user explicitly wants to replace the existing configuration.
