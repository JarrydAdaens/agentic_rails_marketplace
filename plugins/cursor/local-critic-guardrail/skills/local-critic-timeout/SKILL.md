---
name: local-critic-timeout
description: View or persist the local critic advisory consult timeout.
---
# Local critic timeout

Run `uv run --no-project python ./cli/critic_timeout.py --seconds "<USER_SELECTION>" --workspace <PROJECT_ROOT>`. Without seconds, explain the current timeout. Accept a positive number, `default`, `cancel`, or `nevermind`.
